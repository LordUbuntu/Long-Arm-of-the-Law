using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HammerController : MonoBehaviour {
    // TODO
    // - have an arm connecting it to it's "body" (PlayerController)
    // - intertia (track the mouse lazily)
    // - detect overlapping entities on lmb and score them
    
    // XXX
    // What should this be implemented as?
    // Should this be part of PlayerController instead?
}
